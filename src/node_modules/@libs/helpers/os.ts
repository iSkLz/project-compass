import os from 'os';

export const enum OSType {
    linux = 0, win = 1, mac = 2
}

var type: OSType;
var isWindows: boolean = false;
var isLinux: boolean = false;
var isMac: boolean = false;

switch (os.type().toLowerCase()) {
    case "darwin":
        type = OSType.mac;
        isMac = true;
    case "windows_nt":
        type = OSType.win;
        isWindows = true;
    default:
        type = OSType.linux;
        isLinux = true;
}

export {
    type, isWindows, isLinux, isMac
};